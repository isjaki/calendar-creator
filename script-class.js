const DAYS_OF_WEEK = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

class Calendar {
    constructor(options) {
        this._year = options.year;
        this._month = options.month;
        this._
    }
}